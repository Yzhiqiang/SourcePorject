<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.source.sourceporject.mapper.valueMapper">
    <insert id="save" parameterType="com.source.sourceporject.entity.Value" >
        insert into vueblog.sortdata(`values`, id, sorted_values) value(#{values}, #{id}, #{sortedValues})
    </insert>
</mapper>