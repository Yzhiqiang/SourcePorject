server:
  port: 8001
spring:
  application:
    name: sortSystem
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vueblog?serverTimezone=GMT%2B8
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
mybatis:
  mapper-locations: classpath:mapper/*.xml